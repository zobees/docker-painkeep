deathmatch 3
hostname "PainKeep Server"
timelimit 25
fraglimit 75
maxclients 8
serverinfo PainKeep "http://www.planetquake.com/skins/painkeep"

// If the map is ever set to start (the hub map) then hub mode will
// be automatically turned on. This means that and the end of each
// level it will return to the hub again.
map start
